# backend

Ainda não existe. Aqui vai entrar futuramente a lógica P2P do projeto,
construída sobre **Hyperswarm**, **Hypercore** e **Hyperdht** (ecossistema
Holepunch — a mesma base do Keet), responsável por descoberta de peers e
replicação de dados sem servidor.

Essa camada só começa a ser desenvolvida depois que o `frontend/` (que
hoje roda isolado, com dados mockados) estiver validado. Contexto completo
do projeto em `../CLAUDE.md`.
